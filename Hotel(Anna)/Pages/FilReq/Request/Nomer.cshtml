@page
@model Hotel_Anna_.Pages.FilReq.Request.NomerModel
@{
    ViewData["Title"] = "Номера";
}
<table class="table table-hover">
    <thead>
        <tr>
            <th>
                Сотрудник
            </th>
            <th>
                Номер
            </th>
        </tr>
    </thead>
    <tbody>
        @for (int num = 0; num < Model.Employees.Count; num++)
        {
            var item = Model.Employees[num];
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.FIO)
                </td>

                <td>
                    @{
                        try
                        {
                            @Model.Rooms.First(r => r.ID == item.ID).Appellation
                        }
                        catch
                        {
                            <p>нет</p>
                        }
                    }
                </td>
            </tr>
        }
    </tbody>
</table>